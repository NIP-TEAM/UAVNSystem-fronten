import { CSSProperties } from "react";

export const LoginLayoutStyle: Readonly<CSSProperties> = {
    width: '100vw',
    minHeight: '100vh',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
}